# =====================================================
# Adopta — variáveis de ambiente
# Copie este arquivo para .env e ajuste os valores.
# =====================================================

# --- MySQL ---
DB_ROOT_PASSWORD=root_super_secreto
DB_NAME=adopta
DB_USER=adopta
DB_PASSWORD=adopta_dev_senha
DB_HOST=db
DB_PORT=3306

# --- Backend ---
BACKEND_PORT=3000
JWT_SECRET=troque_isso_em_producao_por_uma_chave_aleatoria_longa
JWT_EXPIRACAO=7d

# --- Frontend ---
FRONTEND_PORT=4200
API_URL=http://localhost:3000/api
